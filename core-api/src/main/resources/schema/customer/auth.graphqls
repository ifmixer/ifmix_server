type UserInfo {
    id: UUID!
    email: String
    displayName: String
    avatarUrl: String
}

type LoginPayload {
    accessToken: String!
    refreshToken: String!
    expiresIn: Int!
    user: UserInfo!
    deviceSecret: String
}

type MePayload {
    user: UserInfo!
    "付费等级。100=FREE, 200=PRO, 300=ENTERPRISE"
    tier: Int!
    tierActive: Boolean!
    tierExpiresAt: DateTime
}

type RefreshPayload {
    accessToken: String!
    refreshToken: String!
    expiresIn: Int!
}

type ExchangePayload {
    accessToken: String!
    refreshToken: String!
    expiresIn: Int!
}

input ProviderLoginInput {
    idToken: String!
}

input WechatLoginInput {
    code: String!
}

input ExchangeInput {
    deviceSecret: String!
    targetAppId: UUID!
}

input RefreshInput {
    refreshToken: String!
}

input LogoutInput {
    refreshToken: String!
}

extend type Query {
    q_auth_me: MePayload!
}

extend type Mutation {
    m_auth_loginGoogle(input: ProviderLoginInput!): LoginPayload!
    m_auth_loginApple(input: ProviderLoginInput!): LoginPayload!
    m_auth_loginWechat(input: WechatLoginInput!): LoginPayload!
    m_auth_loginAnonymous: LoginPayload!
    m_auth_exchangeToken(input: ExchangeInput!): ExchangePayload!
    m_auth_refreshToken(input: RefreshInput!): RefreshPayload!
    m_auth_logout(input: LogoutInput!): OperationResult!
    m_auth_deleteAccount: OperationResult!
}
