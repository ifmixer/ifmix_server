# 认证（社交登录）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或
> superpowers:executing-plans 逐任务实现此计划。步骤用复选框（`- [ ]`）跟踪进度。
>
> **项目约定：** 本仓库 commit 交用户 review（不自动 push）；下方 commit 步骤按 TDD 频繁提交的惯例
> 保留，实际由执行者按需提交、交用户确认。

**目标：** 实现规格 `docs/superpowers/specs/2026-07-27-auth-social-login-design.md` 的认证部分：
Google/Apple 原生 id_token 登录 → 无状态 app 级 EdDSA JWT + 不透明 refresh + 跨 app `device_secret`
SSO，落身份到租户级 `auth_identity` + app 级 `app_user`，登录后 `mergeOnLogin` 归并匿名 scan/订阅。

**架构：** provider id_token 验证用 `spring-security-oauth2-jose` 的 `NimbusJwtDecoder`（JWKS 缓存 +
iss/exp 校验，`aud` 在 decoder 外按本 app 配置校验）；自家 Ed25519 access JWT + JWKS 端点用 Nimbus
（+ Tink）；`auth_*` 租户级自定义 repo（`authTenantId` 分片）、`app_*` app 级（`appId` 分片）；refresh
原子轮换 + 登录写用 `TxRunner`（副本集事务）；鉴权用非阻塞拦截器 `populateAuth` 填 `ctx.userId`；
`mergeOnLogin` 用 Spring `@Async @EventListener`。

**技术栈：** Kotlin 2.3.10、Spring Boot 4.1、`spring-security-oauth2-jose`（`NimbusJwtDecoder`）、
`com.nimbusds:nimbus-jose-jwt` + `com.google.crypto.tink:tink`（Ed25519 OKP 签发/JWKS）、Spring Data
MongoDB、虚拟线程。

**环境约束：** 本机无 Docker/Mongo。集成测试（Testcontainers Mongo 副本集）**照写，仅 CI 跑**；
Nimbus 签验、provider 验证（注入本地 JWKS）、EmailNormalize、refresh 轮换（mock repo）、中间件、JWKS
端点等**纯逻辑单元测试可本地跑**。每步用 `./gradlew :core-api:compileKotlin :core-api:compileTestKotlin`
编译验证，单测用 `--tests` 过滤跑。

---

## 前置

- 已实现的地基（`BaseDocument`/`BaseAppDocument`/`AppScoped`/`CRUDRepository`/`CRUDService`、`Instant`
  时间戳、`RequestContext`（`userId` 已预留）、`RequestContextArgumentResolver`、`HeaderValidationInterceptor`、
  `WebConfig`、`ErrorCode`/`ApiError`/`Envelope`/`GlobalExceptionHandler`、`TxRunner`、`MongoClusterResolver`）。
- 已实现的 appconfig（`AppConfigRepo.getByAppId`、`AppConfigDocument` 含 `authTenantId`/`apple.servicesId`/
  `appleBundleId`/`google.clientIds`）。
- 已实现的 antique（`ScanRecordDocument`：`appId`/`installId`/`userId`）、iap（`SubscriptionDocument`：
  同）——`mergeOnLogin` 回填目标。
- 已实现的 antique 限流 `common/ratelimit/resolveSubject`（现为 `userId ?? installId ?? ip`）——任务组 6
  收紧为 `userId ?? ip`（D5）。

## 文件结构

```
build.gradle.kts                          # 加 spring-security-oauth2-jose、nimbus-jose-jwt、tink
common/auth/
  EmailNormalize.kt                       # normalizeEmail / normalizePhone（纯函数）
  Hashing.kt                              # sha256Base64Url、randomToken（SecureRandom）
  AuthJwtKeys.kt                          # 从 env 载入 Ed25519 OKP（私钥 + kid）
  AuthJwtService.kt                       # signAccess / verifyAccess / jwkSetJson
  AuthInterceptor.kt                      # populateAuth（非阻塞，验签→请求属性 authUserId）
modules/auth/
  AuthTenantDocument.kt / AuthIdentityDocument.kt / AuthProviderIdentityDocument.kt
  AuthDeviceSecretDocument.kt / AppUserDocument.kt / AppRefreshTokenDocument.kt
  AuthTenantRepo.kt / AuthIdentityRepo.kt / AuthProviderIdentityRepo.kt
  AuthDeviceSecretRepo.kt / AppUserRepo.kt / AppRefreshTokenRepo.kt
  ProviderVerifier.kt                     # 接口 + VerifiedProvider；GoogleVerifier / AppleVerifier
  AuthDtos.kt                             # 请求/响应 DTO
  AuthService.kt                          # loginWithProvider / exchange / refresh / logout / me
  AuthLoggedInEvent.kt / MergeOnLoginListener.kt
  AuthConfig.kt                           # bean 装配（decoders、jwt、repos、service）
bff/customer/CustomerAuthController.kt    # google/apple/exchange/refresh/logout/me
bff/wellknown/JwksController.kt           # GET /.well-known/jwks.json（公开）
config/WebConfig.kt（改）                 # 注册 AuthInterceptor；排除 /.well-known
config/OpenApiConfig.kt（改）             # 无需 x-app-id 的公开路径（/.well-known）
common/http/RequestContextArgumentResolver.kt（改）  # 读 authUserId → ctx.userId
common/ratelimit/*（改，任务组6）         # resolveSubject 收紧 userId ?? ip
```

> 说明：`auth_*` 集合租户级、自定义 repo（直接 `MongoTemplate`，`authTenantId` 分片，与 `AppConfigRepo`
> 同风格）；`app_user`/`app_refresh_token` 亦用自定义 repo（`ensure`/原子轮换需定制查询），文档用
> `BaseDocument + AppScoped`（无软删；refresh 用自有 `revokedAt`）。

---

## 任务组 1：依赖 + 数据模型 + repo

### 任务 1.1：加依赖

**文件：** 修改 `core-api/build.gradle.kts`

- [ ] **步骤 1：dependencies 加入**

```kotlin
implementation("org.springframework.security:spring-security-oauth2-jose")
implementation("com.nimbusds:nimbus-jose-jwt:9.40")        // 版本以 BOM/最新稳定为准
implementation("com.google.crypto.tink:tink:1.15.0")       // Nimbus Ed25519(OKP) 依赖 Tink
```
> `spring-security-oauth2-jose` 会引入 spring-security-core（`NimbusJwtDecoder`/`JwtIssuerValidator`
> 等），但**不**引入 web 过滤链自动配置（我们不加 `spring-boot-starter-security`，故不会开启默认
> Security 过滤器；鉴权仍走我们自己的拦截器）。若发现引入了 security 自动配置，排除
> `spring-boot-starter-security` 的 auto-config 或不依赖该 starter，仅用 jose 库。

- [ ] **步骤 2：编译**：`./gradlew :core-api:compileKotlin`。预期 PASS。

- [ ] **步骤 3：Commit**：`git add core-api/build.gradle.kts && git commit -m "chore(auth): add jose/nimbus/tink deps"`

### 任务 1.2：文档（documents）

**文件：** 创建 `modules/auth/AuthTenantDocument.kt` 等 6 个文档文件。

- [ ] **步骤 1：`AuthTenantDocument`**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.db.BaseDocument
import org.springframework.data.mongodb.core.index.Indexed
import org.springframework.data.mongodb.core.mapping.Document

/** 租户/身份联邦单位（_id = tenantId）。不软删、不分片——注册表。 */
@Document(collection = "auth_tenant")
class AuthTenantDocument : BaseDocument() {
    var name: String? = null

    @Indexed(unique = true, sparse = true)
    var slug: String? = null
    var desc: String? = null
}
```

- [ ] **步骤 2：`AuthIdentityDocument`**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.db.BaseDocument
import org.springframework.data.mongodb.core.index.CompoundIndex
import org.springframework.data.mongodb.core.index.CompoundIndexes
import org.springframework.data.mongodb.core.mapping.Document

/** 租户级"人"+资料。authTenantId 分片；v1 不按 email/phone 唯一、不自动合并。 */
@Document(collection = "auth_identity")
@CompoundIndexes(
    CompoundIndex(name = "identity_tenant_idx", def = "{'authTenantId': 1, '_id': 1}"),
    CompoundIndex(name = "identity_tenant_email_idx", def = "{'authTenantId': 1, 'email': 1}"),
    CompoundIndex(name = "identity_tenant_phone_idx", def = "{'authTenantId': 1, 'phone': 1}"),
)
class AuthIdentityDocument : BaseDocument() {
    var authTenantId: String? = null
    var rawEmail: String? = null
    var email: String? = null
    var rawPhone: String? = null
    var phone: String? = null
    var contactEmail: String? = null
    var displayName: String? = null
    var passwordHash: String? = null          // 预留
    var profile: Map<String, Any?>? = null
    var metadata: Map<String, Any?>? = null
}
```

- [ ] **步骤 3：`AuthProviderIdentityDocument`**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.db.BaseDocument
import org.springframework.data.mongodb.core.index.CompoundIndex
import org.springframework.data.mongodb.core.index.CompoundIndexes
import org.springframework.data.mongodb.core.mapping.Document

/** provider 维度用户。唯一 (authTenantId, provider, providerAccountId)。 */
@Document(collection = "auth_provider_identity")
@CompoundIndexes(
    CompoundIndex(
        name = "provider_identity_uq",
        def = "{'authTenantId': 1, 'provider': 1, 'providerAccountId': 1}",
        unique = true,
    ),
    CompoundIndex(name = "provider_identity_identity_idx", def = "{'authTenantId': 1, 'authIdentityId': 1}"),
)
class AuthProviderIdentityDocument : BaseDocument() {
    var authTenantId: String? = null
    var authIdentityId: String? = null
    var provider: String? = null              // "google" | "apple"
    var providerAccountId: String? = null     // oauth sub
    var email: String? = null
    var emailVerified: Boolean = false
    var phone: String? = null
    var userMetadata: Map<String, Any?>? = null
    var providerMetadata: Map<String, Any?>? = null
    var loginIp: String? = null
    var loginInstallId: String? = null
    var loginAppId: String? = null
}
```

- [ ] **步骤 4：`AuthDeviceSecretDocument`**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.db.BaseDocument
import org.springframework.data.mongodb.core.index.CompoundIndex
import org.springframework.data.mongodb.core.index.CompoundIndexes
import org.springframework.data.mongodb.core.mapping.Document
import java.time.Instant

/** 设备+租户级引导钥匙。唯一 (authTenantId, secretHash)（分片键前缀）。稳定不轮换。 */
@Document(collection = "auth_device_secret")
@CompoundIndexes(
    CompoundIndex(name = "device_secret_uq", def = "{'authTenantId': 1, 'secretHash': 1}", unique = true),
    CompoundIndex(name = "device_secret_identity_idx", def = "{'authTenantId': 1, 'authIdentityId': 1}"),
)
class AuthDeviceSecretDocument : BaseDocument() {
    var authTenantId: String? = null
    var authIdentityId: String? = null
    var secretHash: String? = null
    var loginInstallId: String? = null
    var expiresAt: Instant? = null
    var revokedAt: Instant? = null
    var lastUsedAt: Instant? = null
}
```

- [ ] **步骤 5：`AppUserDocument`（app 级）**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.db.AppScoped
import com.ifmix.api.core.common.db.BaseDocument
import org.springframework.data.mongodb.core.index.CompoundIndex
import org.springframework.data.mongodb.core.mapping.Document

/** app 级用户（_id = 业务 userId）。唯一 (appId, authIdentityId)。 */
@Document(collection = "app_user")
@CompoundIndex(name = "app_user_uq", def = "{'appId': 1, 'authIdentityId': 1}", unique = true)
class AppUserDocument : BaseDocument(), AppScoped {
    override var appId: String? = null
    var authIdentityId: String? = null
    var metadata: Map<String, Any?>? = null
}
```

- [ ] **步骤 6：`AppRefreshTokenDocument`（app 级）**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.db.AppScoped
import com.ifmix.api.core.common.db.BaseDocument
import org.springframework.data.mongodb.core.index.CompoundIndex
import org.springframework.data.mongodb.core.index.CompoundIndexes
import org.springframework.data.mongodb.core.mapping.Document
import java.time.Instant

/** app 级不透明 refresh。唯一 (appId, tokenHash)（分片键前缀）。自有 revokedAt（非软删）。 */
@Document(collection = "app_refresh_token")
@CompoundIndexes(
    CompoundIndex(name = "refresh_uq", def = "{'appId': 1, 'tokenHash': 1}", unique = true),
    CompoundIndex(name = "refresh_appuser_idx", def = "{'appId': 1, 'appUserId': 1}"),
    CompoundIndex(name = "refresh_device_idx", def = "{'deviceSecretId': 1}"),
)
class AppRefreshTokenDocument : BaseDocument(), AppScoped {
    override var appId: String? = null
    var appUserId: String? = null
    var deviceSecretId: String? = null
    var tokenHash: String? = null
    var loginInstallId: String? = null
    var expiresAt: Instant? = null
    var revokedAt: Instant? = null
    var replacedBy: String? = null
}
```

- [ ] **步骤 7：编译**：`./gradlew :core-api:compileKotlin`。预期 PASS。

- [ ] **步骤 8：Commit**：`git add core-api/.../modules/auth/*Document.kt && git commit -m "feat(auth): documents"`

### 任务 1.3：EmailNormalize + Hashing（纯函数，可本地单测）

**文件：** 创建 `common/auth/EmailNormalize.kt`、`common/auth/Hashing.kt`；测试 `common/auth/EmailNormalizeTest.kt`、`common/auth/HashingTest.kt`

- [ ] **步骤 1：写失败测试 `EmailNormalizeTest`**

```kotlin
package com.ifmix.api.core.common.auth

import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

class EmailNormalizeTest {
    @Test fun `lowercases and strips plus tag`() {
        assertThat(EmailNormalize.normalizeEmail("Foo+tag@Example.com")).isEqualTo("foo@example.com")
    }
    @Test fun `gmail strips dots and unifies domain`() {
        assertThat(EmailNormalize.normalizeEmail("f.o.o+x@googlemail.com")).isEqualTo("foo@gmail.com")
    }
    @Test fun `null or blank returns null`() {
        assertThat(EmailNormalize.normalizeEmail(null)).isNull()
        assertThat(EmailNormalize.normalizeEmail("  ")).isNull()
    }
    @Test fun `phone strips separators keeps leading plus`() {
        assertThat(EmailNormalize.normalizePhone("+1 (650) 555-1234")).isEqualTo("+16505551234")
    }
}
```

运行：`./gradlew :core-api:test --tests "*EmailNormalizeTest"`。预期 FAIL。

- [ ] **步骤 2：实现 `EmailNormalize`**

```kotlin
package com.ifmix.api.core.common.auth

/** 邮箱/电话规范化（纯函数）。v1 phone 仅去分隔符+保留前导+；完整 E.164 校验留后续（libphonenumber）。 */
object EmailNormalize {
    fun normalizeEmail(raw: String?): String? {
        val s = raw?.trim()?.lowercase()?.takeIf { it.isNotEmpty() } ?: return null
        val at = s.indexOf('@')
        if (at <= 0 || at == s.length - 1) return s
        var local = s.substring(0, at)
        var domain = s.substring(at + 1)
        val plus = local.indexOf('+')
        if (plus >= 0) local = local.substring(0, plus)
        if (domain == "gmail.com" || domain == "googlemail.com") {
            local = local.replace(".", "")
            domain = "gmail.com"
        }
        return "$local@$domain"
    }

    fun normalizePhone(raw: String?): String? {
        val s = raw?.trim()?.takeIf { it.isNotEmpty() } ?: return null
        val plus = if (s.startsWith("+")) "+" else ""
        return plus + s.filter { it.isDigit() }
    }
}
```

运行：`./gradlew :core-api:test --tests "*EmailNormalizeTest"`。预期 PASS。

- [ ] **步骤 3：写失败测试 `HashingTest`**

```kotlin
package com.ifmix.api.core.common.auth

import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

class HashingTest {
    @Test fun `sha256 is deterministic base64url no padding`() {
        val a = Hashing.sha256Base64Url("hello")
        assertThat(a).isEqualTo(Hashing.sha256Base64Url("hello"))
        assertThat(a).doesNotContain("=", "+", "/")
    }
    @Test fun `randomToken length and charset`() {
        val t = Hashing.randomTokenBase64Url(32)
        assertThat(t).doesNotContain("=", "+", "/")
        assertThat(Hashing.randomTokenBase64Url(32)).isNotEqualTo(t)
    }
}
```

- [ ] **步骤 4：实现 `Hashing`**

```kotlin
package com.ifmix.api.core.common.auth

import java.security.MessageDigest
import java.security.SecureRandom
import java.util.Base64

/** sha256(base64url) 与随机 token 生成。 */
object Hashing {
    private val rng = SecureRandom()
    private val b64 = Base64.getUrlEncoder().withoutPadding()

    fun sha256Base64Url(input: String): String =
        b64.encodeToString(MessageDigest.getInstance("SHA-256").digest(input.toByteArray(Charsets.UTF_8)))

    fun randomTokenBase64Url(bytes: Int = 32): String {
        val buf = ByteArray(bytes)
        rng.nextBytes(buf)
        return b64.encodeToString(buf)
    }
}
```

运行：`./gradlew :core-api:test --tests "*HashingTest"`。预期 PASS。

- [ ] **步骤 5：Commit**：`git commit -am "feat(auth): email/phone normalize + hashing"`

### 任务 1.4：Repos（自定义 MongoTemplate）

**文件：** 创建 `modules/auth/AuthTenantRepo.kt`、`AuthProviderIdentityRepo.kt`、`AuthDeviceSecretRepo.kt`、
`AppUserRepo.kt`、`AppRefreshTokenRepo.kt`（`AuthIdentityRepo` 的创建/查在 `AuthProviderIdentityRepo.upsert`
内完成，不单列）。测试：`AppRefreshTokenRepoRotateTest`（用 Testcontainers；本地仅编译）；轮换的**原子性
断言**放集成测试。

> 这些 repo 直接持 `MongoTemplate`（`@Component` 注入），与 `AppConfigRepo` 同风格。租户级查询恒带
> `authTenantId`；app 级恒带 `appId`（分片定向）。

- [ ] **步骤 1：`AuthTenantRepo`**

```kotlin
package com.ifmix.api.core.modules.auth

import org.bson.types.ObjectId
import org.springframework.data.mongodb.core.MongoTemplate
import org.springframework.stereotype.Component

@Component
class AuthTenantRepo(private val mongo: MongoTemplate) {
    fun findById(tenantId: String): AuthTenantDocument? =
        if (ObjectId.isValid(tenantId)) mongo.findById(tenantId, AuthTenantDocument::class.java) else null
}
```

- [ ] **步骤 2：`AuthProviderIdentityRepo.upsert`（建/复用 identity + provider_identity）**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.auth.EmailNormalize
import org.springframework.data.mongodb.core.MongoTemplate
import org.springframework.data.mongodb.core.query.Criteria
import org.springframework.data.mongodb.core.query.Query
import org.springframework.data.mongodb.core.query.Update
import org.springframework.data.mongodb.core.query.isEqualTo
import org.springframework.stereotype.Component
import java.time.Instant

data class UpsertInput(
    val provider: String,
    val accountId: String,
    val email: String?,
    val emailVerified: Boolean,
    val phone: String?,
    val userMetadata: Map<String, Any?>?,
    val loginIp: String?,
    val loginInstallId: String?,
    val loginAppId: String?,
)

@Component
class AuthProviderIdentityRepo(private val mongo: MongoTemplate) {

    /** 返回 authIdentityId。已存在 provider 账号 → 复用其 identity 并更新登录元信息；否则新建 identity + provider_identity。 */
    fun upsert(tenantId: String, input: UpsertInput): String {
        val now = Instant.now()
        val existing = mongo.findOne(
            Query(
                Criteria.where("authTenantId").isEqualTo(tenantId)
                    .and("provider").isEqualTo(input.provider)
                    .and("providerAccountId").isEqualTo(input.accountId),
            ),
            AuthProviderIdentityDocument::class.java,
        )
        if (existing?.authIdentityId != null) {
            mongo.updateFirst(
                Query(Criteria.where("_id").isEqualTo(org.bson.types.ObjectId(existing.id))),
                Update().set("email", input.email).set("emailVerified", input.emailVerified)
                    .set("phone", input.phone).set("loginIp", input.loginIp)
                    .set("loginInstallId", input.loginInstallId).set("loginAppId", input.loginAppId)
                    .set("userMetadata", input.userMetadata).set("updatedAt", now),
                AuthProviderIdentityDocument::class.java,
            )
            return existing.authIdentityId!!
        }
        // 新建 identity（v1：每次新 provider 账号 = 新 identity，不按 email 合并）
        val identity = AuthIdentityDocument().apply {
            authTenantId = tenantId
            rawEmail = input.email
            email = EmailNormalize.normalizeEmail(input.email)
            rawPhone = input.phone
            phone = EmailNormalize.normalizePhone(input.phone)
            displayName = input.userMetadata?.get("name") as? String
            profile = input.userMetadata
            createdAt = now; updatedAt = now
        }
        mongo.insert(identity)
        val pi = AuthProviderIdentityDocument().apply {
            authTenantId = tenantId
            authIdentityId = identity.id
            provider = input.provider
            providerAccountId = input.accountId
            email = input.email
            emailVerified = input.emailVerified
            phone = input.phone
            userMetadata = input.userMetadata
            loginIp = input.loginIp; loginInstallId = input.loginInstallId; loginAppId = input.loginAppId
            createdAt = now; updatedAt = now
        }
        mongo.insert(pi)
        return identity.id!!
    }
}
```

- [ ] **步骤 3：`AppUserRepo.ensure`**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.http.RequestContext
import org.springframework.dao.DuplicateKeyException
import org.springframework.data.mongodb.core.MongoTemplate
import org.springframework.data.mongodb.core.query.Criteria
import org.springframework.data.mongodb.core.query.Query
import org.springframework.data.mongodb.core.query.isEqualTo
import org.springframework.stereotype.Component
import java.time.Instant

@Component
class AppUserRepo(private val mongo: MongoTemplate) {
    /** 为 (appId, authIdentityId) 建/取 app_user，返回 appUserId。并发靠唯一索引兜底后回读。 */
    fun ensure(appId: String, authIdentityId: String): String {
        findId(appId, authIdentityId)?.let { return it }
        val now = Instant.now()
        val doc = AppUserDocument().apply {
            this.appId = appId; this.authIdentityId = authIdentityId; createdAt = now; updatedAt = now
        }
        return try {
            mongo.insert(doc); doc.id!!
        } catch (e: DuplicateKeyException) {
            findId(appId, authIdentityId) ?: throw e
        }
    }

    private fun findId(appId: String, authIdentityId: String): String? = mongo.findOne(
        Query(Criteria.where("appId").isEqualTo(appId).and("authIdentityId").isEqualTo(authIdentityId)),
        AppUserDocument::class.java,
    )?.id
}
```

- [ ] **步骤 4：`AuthDeviceSecretRepo`**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.auth.Hashing
import org.springframework.data.mongodb.core.MongoTemplate
import org.springframework.data.mongodb.core.query.Criteria
import org.springframework.data.mongodb.core.query.Query
import org.springframework.data.mongodb.core.query.Update
import org.springframework.data.mongodb.core.query.isEqualTo
import org.springframework.stereotype.Component
import java.time.Duration
import java.time.Instant

@Component
class AuthDeviceSecretRepo(private val mongo: MongoTemplate) {
    private val idleTtl = Duration.ofDays(90)

    /** mint 新 device_secret，返回 (id, 明文 secret)。明文只此一次可见。 */
    fun issue(tenantId: String, authIdentityId: String, loginInstallId: String?): Pair<String, String> {
        val secret = Hashing.randomTokenBase64Url(32)
        val now = Instant.now()
        val doc = AuthDeviceSecretDocument().apply {
            authTenantId = tenantId; this.authIdentityId = authIdentityId
            secretHash = Hashing.sha256Base64Url(secret); loginInstallId = loginInstallId
            expiresAt = now.plus(idleTtl); lastUsedAt = now; createdAt = now; updatedAt = now
        }
        mongo.insert(doc)
        return doc.id!! to secret
    }

    /** 按 (tenantId, hash) 定向查有效 device_secret（未撤销、未过期）。 */
    fun findValid(tenantId: String, secretPlain: String): AuthDeviceSecretDocument? {
        val doc = mongo.findOne(
            Query(
                Criteria.where("authTenantId").isEqualTo(tenantId)
                    .and("secretHash").isEqualTo(Hashing.sha256Base64Url(secretPlain)),
            ),
            AuthDeviceSecretDocument::class.java,
        ) ?: return null
        val valid = doc.revokedAt == null && (doc.expiresAt?.isAfter(Instant.now()) == true)
        return if (valid) doc else null
    }

    fun touch(id: String) {
        val now = Instant.now()
        mongo.updateFirst(
            Query(Criteria.where("_id").isEqualTo(org.bson.types.ObjectId(id))),
            Update().set("lastUsedAt", now).set("expiresAt", now.plus(idleTtl)).set("updatedAt", now),
            AuthDeviceSecretDocument::class.java,
        )
    }

    fun revoke(id: String) {
        val now = Instant.now()
        mongo.updateFirst(
            Query(Criteria.where("_id").isEqualTo(org.bson.types.ObjectId(id))),
            Update().set("revokedAt", now).set("updatedAt", now),
            AuthDeviceSecretDocument::class.java,
        )
    }
}
```

- [ ] **步骤 5：`AppRefreshTokenRepo`（原子轮换）**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.auth.Hashing
import org.bson.types.ObjectId
import org.springframework.data.mongodb.core.MongoTemplate
import org.springframework.data.mongodb.core.query.Criteria
import org.springframework.data.mongodb.core.query.Query
import org.springframework.data.mongodb.core.query.Update
import org.springframework.data.mongodb.core.query.isEqualTo
import org.springframework.stereotype.Component
import java.time.Duration
import java.time.Instant

class RefreshIssued(val id: String, val token: String, val expiresAt: Instant)

@Component
class AppRefreshTokenRepo(private val mongo: MongoTemplate) {
    private val ttl = Duration.ofDays(30)

    /** id 可预生成（供 refresh 原子轮换：先 tryRotate 置 replacedBy=newId 胜出后再以该 id 插入，无孤儿）。 */
    fun issue(appId: String, appUserId: String, deviceSecretId: String, loginInstallId: String?, id: String? = null): RefreshIssued {
        val token = Hashing.randomTokenBase64Url(32)
        val now = Instant.now()
        val exp = now.plus(ttl)
        val doc = AppRefreshTokenDocument().apply {
            this.id = id
            this.appId = appId; this.appUserId = appUserId; this.deviceSecretId = deviceSecretId
            tokenHash = Hashing.sha256Base64Url(token); loginInstallId = loginInstallId
            expiresAt = exp; createdAt = now; updatedAt = now
        }
        mongo.insert(doc)
        return RefreshIssued(doc.id!!, token, exp)
    }

    fun findByHash(appId: String, tokenPlain: String): AppRefreshTokenDocument? = mongo.findOne(
        Query(Criteria.where("appId").isEqualTo(appId).and("tokenHash").isEqualTo(Hashing.sha256Base64Url(tokenPlain))),
        AppRefreshTokenDocument::class.java,
    )

    /** 原子轮换：条件 updateFirst 只在未撤销时置 revokedAt+replacedBy。返回是否本方胜出（modifiedCount>0）。 */
    fun tryRotate(appId: String, tokenHash: String, newId: String): Boolean {
        val now = Instant.now()
        val res = mongo.updateFirst(
            Query(Criteria.where("appId").isEqualTo(appId).and("tokenHash").isEqualTo(tokenHash).and("revokedAt").isEqualTo(null)),
            Update().set("revokedAt", now).set("replacedBy", newId).set("updatedAt", now),
            AppRefreshTokenDocument::class.java,
        )
        return res.modifiedCount > 0
    }

    fun revokeByAppUser(appId: String, appUserId: String) {
        val now = Instant.now()
        mongo.updateMulti(
            Query(Criteria.where("appId").isEqualTo(appId).and("appUserId").isEqualTo(appUserId).and("revokedAt").isEqualTo(null)),
            Update().set("revokedAt", now).set("updatedAt", now),
            AppRefreshTokenDocument::class.java,
        )
    }

    /** 登出：撤销某 deviceSecret 关联的全部未撤销 refresh（跨该设备所有 app）。 */
    fun revokeByDeviceSecret(deviceSecretId: String) {
        val now = Instant.now()
        mongo.updateMulti(
            Query(Criteria.where("deviceSecretId").isEqualTo(deviceSecretId).and("revokedAt").isEqualTo(null)),
            Update().set("revokedAt", now).set("updatedAt", now),
            AppRefreshTokenDocument::class.java,
        )
    }
}
```

- [ ] **步骤 6：编译**：`./gradlew :core-api:compileKotlin`。预期 PASS。

- [ ] **步骤 7：Commit**：`git commit -am "feat(auth): repos"`

---

## 任务组 2：JWT / JWKS（Nimbus Ed25519）

### 任务 2.1：AuthJwtKeys（Ed25519 密钥载入）

**文件：** 创建 `common/auth/AuthJwtKeys.kt`；测试 `common/auth/AuthJwtServiceTest.kt`（下一任务合并测试）。

- [ ] **步骤 1：实现 `AuthJwtKeys`**

```kotlin
package com.ifmix.api.core.common.auth

import com.nimbusds.jose.jwk.Curve
import com.nimbusds.jose.jwk.JWKSet
import com.nimbusds.jose.jwk.OctetKeyPair
import com.nimbusds.jose.jwk.gen.OctetKeyPairGenerator

/**
 * 持有 Ed25519 签名密钥（OKP，含私钥）+ kid。私钥来自 env AUTH_JWT_PRIVATE_KEY（JWK JSON）；
 * 缺省时生成临时密钥（仅本地/测试，日志告警）。支持多公钥（kid → 公钥）以备轮换（v1 单钥）。
 */
class AuthJwtKeys(privateJwkJson: String?) {

    val signingKey: OctetKeyPair = if (privateJwkJson.isNullOrBlank()) {
        OctetKeyPairGenerator(Curve.Ed25519).keyID("dev-ephemeral").generate()
    } else {
        OctetKeyPair.parse(privateJwkJson)
    }

    val kid: String = signingKey.keyID

    /** kid → 公钥（验签用）。v1 仅当前一把。 */
    private val publicByKid: Map<String, OctetKeyPair> = mapOf(kid to signingKey.toPublicJWK())

    fun publicKeyFor(kid: String?): OctetKeyPair? = publicByKid[kid]

    /** 对外 JWKS（仅公钥）。 */
    fun jwkSetJson(): String = JWKSet(publicByKid.values.map { it as com.nimbusds.jose.jwk.JWK }).toString()
}
```

- [ ] **步骤 2：编译**：`./gradlew :core-api:compileKotlin`。预期 PASS。

### 任务 2.2：AuthJwtService（签发 / 验签 / JWKS）

**文件：** 创建 `common/auth/AuthJwtService.kt`；测试 `common/auth/AuthJwtServiceTest.kt`

- [ ] **步骤 1：写失败测试 `AuthJwtServiceTest`（本地可跑，临时密钥）**

```kotlin
package com.ifmix.api.core.common.auth

import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

class AuthJwtServiceTest {
    private val svc = AuthJwtService(AuthJwtKeys(null), accessTtlSec = 900)

    @Test fun `sign then verify returns appUserId when aid matches`() {
        val token = svc.signAccess(appUserId = "u1", appId = "app1")
        assertThat(svc.verifyAccess(token, expectedAppId = "app1")).isEqualTo("u1")
    }

    @Test fun `verify returns null when aid mismatches`() {
        val token = svc.signAccess("u1", "app1")
        assertThat(svc.verifyAccess(token, expectedAppId = "other")).isNull()
    }

    @Test fun `verify returns null for garbage token`() {
        assertThat(svc.verifyAccess("not.a.jwt", "app1")).isNull()
    }

    @Test fun `verify returns null when expired`() {
        val shortSvc = AuthJwtService(AuthJwtKeys(null), accessTtlSec = -1) // 已过期
        val token = shortSvc.signAccess("u1", "app1")
        assertThat(shortSvc.verifyAccess(token, "app1")).isNull()
    }

    @Test fun `jwkSet contains public key and no private d`() {
        val json = svc.jwkSetJson()
        assertThat(json).contains("\"kty\":\"OKP\"").contains("\"crv\":\"Ed25519\"")
        assertThat(json).doesNotContain("\"d\":")  // 不泄露私钥
    }
}
```

运行：`./gradlew :core-api:test --tests "*AuthJwtServiceTest"`。预期 FAIL。

- [ ] **步骤 2：实现 `AuthJwtService`**

```kotlin
package com.ifmix.api.core.common.auth

import com.nimbusds.jose.JWSAlgorithm
import com.nimbusds.jose.JWSHeader
import com.nimbusds.jose.crypto.Ed25519Signer
import com.nimbusds.jose.crypto.Ed25519Verifier
import com.nimbusds.jwt.JWTClaimsSet
import com.nimbusds.jwt.SignedJWT
import java.time.Instant
import java.util.Date

/** 自家 app 级 access JWT：EdDSA(Ed25519) 签发/验签 + 暴露公钥 JWKS。 */
class AuthJwtService(private val keys: AuthJwtKeys, private val accessTtlSec: Long = 900) {

    fun signAccess(appUserId: String, appId: String): String {
        val now = Instant.now()
        val claims = JWTClaimsSet.Builder()
            .subject(appUserId)
            .claim("aid", appId)
            .issueTime(Date.from(now))
            .expirationTime(Date.from(now.plusSeconds(accessTtlSec)))
            .build()
        val header = JWSHeader.Builder(JWSAlgorithm.EdDSA).keyID(keys.kid).build()
        val jwt = SignedJWT(header, claims)
        jwt.sign(Ed25519Signer(keys.signingKey))
        return jwt.serialize()
    }

    /** 验签 + exp + aid==expectedAppId。任何失败返回 null（不抛）。成功返回 appUserId(sub)。 */
    fun verifyAccess(token: String, expectedAppId: String): String? = try {
        val jwt = SignedJWT.parse(token)
        val pub = keys.publicKeyFor(jwt.header.keyID) ?: return null
        if (!jwt.verify(Ed25519Verifier(pub))) return null
        val claims = jwt.jwtClaimsSet
        val exp = claims.expirationTime ?: return null
        if (exp.toInstant().isBefore(Instant.now())) return null
        if (claims.getStringClaim("aid") != expectedAppId) return null
        claims.subject
    } catch (_: Exception) {
        null
    }

    fun jwkSetJson(): String = keys.jwkSetJson()
}
```

运行：`./gradlew :core-api:test --tests "*AuthJwtServiceTest"`。预期 PASS（5 用例）。

- [ ] **步骤 3：Commit**：`git commit -am "feat(auth): Ed25519 JWT sign/verify + JWKS"`

---

## 任务组 3：Provider 验证（`NimbusJwtDecoder`）

### 任务 3.1：ProviderVerifier 接口 + Google/Apple 实现

**文件：** 创建 `modules/auth/ProviderVerifier.kt`；测试 `modules/auth/ProviderVerifierTest.kt`

- [ ] **步骤 1：写失败测试 `ProviderVerifierTest`（注入 stub `JwtDecoder`，本地可跑）**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.http.ApiError
import com.ifmix.api.core.common.http.ClientPlatform
import com.ifmix.api.core.modules.app.AppConfig
import com.ifmix.api.core.modules.app.GoogleClientIds
import org.assertj.core.api.Assertions.assertThat
import org.assertj.core.api.Assertions.assertThatThrownBy
import org.junit.jupiter.api.Test
import org.springframework.security.oauth2.jwt.Jwt
import org.springframework.security.oauth2.jwt.JwtDecoder
import java.time.Instant

private fun cfg() = AppConfig(
    id = "c", appId = "app1", authTenantId = "t1", revision = 1,
    appleBundleId = "com.x.app", androidPackageName = null,
    appleAppAppleId = null, appleIssuerId = null, appleKeyId = null, applePrivateKey = null,
    appleServicesId = "com.x.svc",
    googleServiceAccount = null, googleClientIds = GoogleClientIds(ios = "gid-ios", android = "gid-and", web = "gid-web"),
    productTierMap = emptyMap(), iapEnv = "production", createdAt = null, updatedAt = null,
)

private fun jwt(aud: String, sub: String = "sub123") = Jwt.withTokenValue("t")
    .header("alg", "RS256").subject(sub).audience(listOf(aud))
    .claim("email", "a@b.com").claim("email_verified", true).claim("name", "A B")
    .issuedAt(Instant.now()).expiresAt(Instant.now().plusSeconds(60)).build()

class ProviderVerifierTest {
    @Test fun `google verify ok when aud matches platform client id`() {
        val decoder = JwtDecoder { jwt("gid-ios") }
        val v = GoogleVerifier(decoder).verify(cfg(), ClientPlatform.IOS, "idtoken")
        assertThat(v.accountId).isEqualTo("sub123")
        assertThat(v.email).isEqualTo("a@b.com")
        assertThat(v.emailVerified).isTrue()
    }

    @Test fun `google verify fails when aud mismatches`() {
        val decoder = JwtDecoder { jwt("wrong-aud") }
        assertThatThrownBy { GoogleVerifier(decoder).verify(cfg(), ClientPlatform.IOS, "idtoken") }
            .isInstanceOf(ApiError::class.java)
    }

    @Test fun `apple verify uses bundleId for native`() {
        val decoder = JwtDecoder { jwt("com.x.app") }
        val v = AppleVerifier(decoder).verify(cfg(), ClientPlatform.IOS, "idtoken")
        assertThat(v.accountId).isEqualTo("sub123")
    }

    @Test fun `apple verify uses servicesId for web`() {
        val decoder = JwtDecoder { jwt("com.x.svc") }
        val v = AppleVerifier(decoder).verify(cfg(), ClientPlatform.WEB, "idtoken")
        assertThat(v.accountId).isEqualTo("sub123")
    }

    @Test fun `verify fails when decoder throws`() {
        val decoder = JwtDecoder { throw org.springframework.security.oauth2.jwt.JwtException("bad") }
        assertThatThrownBy { GoogleVerifier(decoder).verify(cfg(), ClientPlatform.IOS, "idtoken") }
            .isInstanceOf(ApiError::class.java)
    }
}
```

运行：`./gradlew :core-api:test --tests "*ProviderVerifierTest"`。预期 FAIL。

- [ ] **步骤 2：实现 `ProviderVerifier`**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.http.ApiError
import com.ifmix.api.core.common.http.ClientPlatform
import com.ifmix.api.core.common.http.ErrorCode
import com.ifmix.api.core.modules.app.AppConfig
import org.springframework.security.oauth2.jwt.Jwt
import org.springframework.security.oauth2.jwt.JwtDecoder

data class VerifiedProvider(
    val accountId: String,
    val email: String?,
    val emailVerified: Boolean,
    val phone: String?,
    val userMetadata: Map<String, Any?>,
)

/** provider id_token 验证接缝。decoder 已做验签 + iss + exp；此处按本 app 配置校验 aud + 取 claims。 */
interface ProviderVerifier {
    val provider: String
    fun verify(config: AppConfig, platform: ClientPlatform?, idToken: String): VerifiedProvider
}

private fun decodeOrFail(decoder: JwtDecoder, idToken: String): Jwt = try {
    decoder.decode(idToken)
} catch (e: Exception) {
    throw ApiError(ErrorCode.AUTH_PROVIDER_FAILED, e.message ?: "provider verify failed")
}

private fun Jwt.toVerified(): VerifiedProvider = VerifiedProvider(
    accountId = subject,
    email = getClaimAsString("email"),
    emailVerified = (getClaim<Any?>("email_verified") as? Boolean) ?: false,
    phone = getClaimAsString("phone_number"),
    userMetadata = mapOf("name" to getClaimAsString("name"), "picture" to getClaimAsString("picture")),
)

private fun requireAud(jwt: Jwt, expected: String?) {
    if (expected.isNullOrBlank() || jwt.audience?.contains(expected) != true) {
        throw ApiError(ErrorCode.AUTH_PROVIDER_FAILED, "aud mismatch")
    }
}

class GoogleVerifier(private val decoder: JwtDecoder) : ProviderVerifier {
    override val provider = "google"
    override fun verify(config: AppConfig, platform: ClientPlatform?, idToken: String): VerifiedProvider {
        val jwt = decodeOrFail(decoder, idToken)
        val aud = when (platform) {
            ClientPlatform.IOS -> config.googleClientIds.ios
            ClientPlatform.ANDROID -> config.googleClientIds.android
            ClientPlatform.WEB, null -> config.googleClientIds.web
        }
        requireAud(jwt, aud)
        return jwt.toVerified()
    }
}

class AppleVerifier(private val decoder: JwtDecoder) : ProviderVerifier {
    override val provider = "apple"
    override fun verify(config: AppConfig, platform: ClientPlatform?, idToken: String): VerifiedProvider {
        val jwt = decodeOrFail(decoder, idToken)
        val aud = if (platform == ClientPlatform.WEB) config.appleServicesId else config.appleBundleId
        requireAud(jwt, aud)
        return jwt.toVerified()
    }
}
```

运行：`./gradlew :core-api:test --tests "*ProviderVerifierTest"`。预期 PASS。

- [ ] **步骤 3：错误码**：确认 `common/http/ErrorCode.kt` 有 `AUTH_PROVIDER_FAILED`；无则加：

```kotlin
AUTH_PROVIDER_FAILED("401001", HttpStatus.UNAUTHORIZED),
```
（`UNAUTHORIZED`、`APP_CONFIG_MISSING` 已在 iap 任务加过。）

- [ ] **步骤 4：Commit**：`git commit -am "feat(auth): provider verifiers (google/apple)"`

---

## 任务组 4：DTO + AuthService（登录/exchange/refresh/logout/me）

### 任务 4.1：DTO + 事件类

**文件：** 创建 `modules/auth/AuthDtos.kt`、`modules/auth/AuthLoggedInEvent.kt`

- [ ] **步骤 1：`AuthDtos`**

```kotlin
package com.ifmix.api.core.modules.auth

import jakarta.validation.constraints.NotBlank
import java.time.Instant

data class UserDto(val id: String?, val email: String?)

data class LoginReq(
    @field:NotBlank val idToken: String? = null,
    val deviceSecret: String? = null,
)
data class LoginRes(
    val accessToken: String,
    val refreshToken: String,
    val refreshExpiresAt: Instant,
    val deviceSecret: String,
    val expiresIn: Long,
    val user: UserDto,
)

data class ExchangeReq(@field:NotBlank val deviceSecret: String? = null)
data class ExchangeRes(
    val accessToken: String,
    val refreshToken: String,
    val refreshExpiresAt: Instant,
    val expiresIn: Long,
    val user: UserDto,
)

data class RefreshReq(@field:NotBlank val refreshToken: String? = null)
data class RefreshRes(
    val accessToken: String,
    val refreshToken: String,
    val refreshExpiresAt: Instant,
    val expiresIn: Long,
)

data class LogoutReq(@field:NotBlank val refreshToken: String? = null)
data class LogoutRes(val ok: Boolean)

data class MeRes(val id: String, val email: String?)
```

- [ ] **步骤 2：`AuthLoggedInEvent`**

```kotlin
package com.ifmix.api.core.modules.auth

/** 登录成功事件：mergeOnLogin 监听，回填匿名 scan/订阅归属。 */
data class AuthLoggedInEvent(val appId: String, val appUserId: String, val installId: String?)
```

- [ ] **步骤 3：编译**：`./gradlew :core-api:compileKotlin`。预期 PASS。

### 任务 4.2：AuthService

**文件：** 创建 `modules/auth/AuthService.kt`；测试 `modules/auth/AuthServiceTest.kt`

- [ ] **步骤 1：写失败测试 `AuthServiceTest`（全 mock，本地可跑）**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.http.ApiError
import com.ifmix.api.core.common.http.ErrorCode
import com.ifmix.api.core.common.http.RequestContext
import com.ifmix.api.core.common.tx.TxRunner
import com.ifmix.api.core.modules.app.AppConfig
import com.ifmix.api.core.modules.app.AppConfigRepo
import com.ifmix.api.core.modules.app.GoogleClientIds
import org.assertj.core.api.Assertions.assertThat
import org.assertj.core.api.Assertions.assertThatThrownBy
import org.junit.jupiter.api.Test
import org.mockito.kotlin.*
import org.springframework.context.ApplicationEventPublisher
import java.time.Instant

private fun ctx() = RequestContext(appId = "app1", installId = "inst1")
private fun appConfig() = AppConfig(
    id = "c", appId = "app1", authTenantId = "t1", revision = 1, appleBundleId = null,
    androidPackageName = null, appleAppAppleId = null, appleIssuerId = null, appleKeyId = null,
    applePrivateKey = null, appleServicesId = null, googleServiceAccount = null,
    googleClientIds = GoogleClientIds(), productTierMap = emptyMap(), iapEnv = "production",
    createdAt = null, updatedAt = null,
)

class AuthServiceTest {
    private val appConfigrepoCtx = mock<AppConfigRepo>()
    private val verifier = mock<ProviderVerifier> { on { provider } doReturn "google" }
    private val providerIdentityrepoCtx = mock<AuthProviderIdentityRepo>()
    private val appUserrepoCtx = mock<AppUserRepo>()
    private val deviceSecretrepoCtx = mock<AuthDeviceSecretRepo>()
    private val refreshrepoCtx = mock<AppRefreshTokenRepo>()
    private val jwt = mock<com.ifmix.api.core.common.auth.AuthJwtService>()
    private val events = mock<ApplicationEventPublisher>()
    private val tx = mock<TxRunner> {
        on { withTx<Any>(any(), any()) } doAnswer { inv ->
            @Suppress("UNCHECKED_CAST")
            (inv.arguments[1] as (RequestContext) -> Any).invoke(inv.arguments[0] as RequestContext)
        }
    }
    private val svc = AuthService(
        appConfigRepo, mapOf("google" to verifier), jwt, providerIdentityRepo, appUserRepo,
        deviceSecretRepo, refreshRepo, tx, events, accessTtlSec = 900,
    )

    @Test fun `login happy path issues tokens and emits event`() {
        whenever(appConfigRepo.getByAppId("app1")).thenReturn(appConfig())
        whenever(verifier.verify(any(), anyOrNull(), any()))
            .thenReturn(VerifiedProvider("acc1", "a@b.com", true, null, emptyMap()))
        whenever(providerIdentityRepo.upsert(eq("t1"), any())).thenReturn("identity1")
        whenever(appUserRepo.ensure("app1", "identity1")).thenReturn("user1")
        whenever(deviceSecretRepo.issue("t1", "identity1", "inst1")).thenReturn("ds1" to "secretPlain")
        whenever(refreshRepo.issue(eq("app1"), eq("user1"), eq("ds1"), eq("inst1"), anyOrNull()))
            .thenReturn(RefreshIssued("r1", "refreshPlain", Instant.now().plusSeconds(100)))
        whenever(jwt.signAccess("user1", "app1")).thenReturn("access.jwt")

        val res = svc.loginWithProvider(ctx(), "google", LoginReq(idToken = "idtok"))

        assertThat(res.accessToken).isEqualTo("access.jwt")
        assertThat(res.refreshToken).isEqualTo("refreshPlain")
        assertThat(res.deviceSecret).isEqualTo("secretPlain")
        assertThat(res.user.id).isEqualTo("user1")
        verify(events).publishEvent(AuthLoggedInEvent("app1", "user1", "inst1"))
    }

    @Test fun `login without app config fails`() {
        whenever(appConfigRepo.getByAppId("app1")).thenReturn(null)
        assertThatThrownBy { svc.loginWithProvider(ctx(), "google", LoginReq(idToken = "x")) }
            .isInstanceOf(ApiError::class.java)
            .extracting("errorCode").isEqualTo(ErrorCode.APP_CONFIG_MISSING)
    }

    @Test fun `refresh replay revokes all and fails`() {
        val row = AppRefreshTokenDocument().apply {
            id = "r1"; appId = "app1"; appUserId = "user1"; deviceSecretId = "ds1"
            revokedAt = Instant.now()  // 已撤销 = 重放
        }
        whenever(refreshRepo.findByHash("app1", "tok")).thenReturn(row)
        assertThatThrownBy { svc.refresh(ctx(), RefreshReq("tok")) }.isInstanceOf(ApiError::class.java)
        verify(refreshRepo).revokeByAppUser("app1", "user1")
    }
}
```

运行：`./gradlew :core-api:test --tests "*AuthServiceTest"`。预期 FAIL。

- [ ] **步骤 2：实现 `AuthService`**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.auth.AuthJwtService
import com.ifmix.api.core.common.http.ApiError
import com.ifmix.api.core.common.http.ErrorCode
import com.ifmix.api.core.common.http.RequestContext
import com.ifmix.api.core.common.tx.TxRunner
import com.ifmix.api.core.modules.app.AppConfigRepo
import org.bson.types.ObjectId
import org.springframework.context.ApplicationEventPublisher
import java.time.Instant

class AuthService(
    private val appConfigRepo: AppConfigRepo,
    private val verifiers: Map<String, ProviderVerifier>,   // "google"/"apple"
    private val jwt: AuthJwtService,
    private val providerIdentityRepo: AuthProviderIdentityRepo,
    private val appUserRepo: AppUserRepo,
    private val deviceSecretRepo: AuthDeviceSecretRepo,
    private val refreshRepo: AppRefreshTokenRepo,
    private val txRunner: TxRunner,
    private val events: ApplicationEventPublisher,
    private val accessTtlSec: Long,
) {
    private fun tenantId(ctx: RequestContext): String =
        appConfigRepo.getByAppId(ctx.appId)?.authTenantId ?: throw ApiError(ErrorCode.APP_CONFIG_MISSING)

    fun loginWithProvider(ctx: RequestContext, provider: String, req: LoginReq): LoginRes {
        val cfg = appConfigRepo.getByAppId(ctx.appId) ?: throw ApiError(ErrorCode.APP_CONFIG_MISSING)
        val tid = cfg.authTenantId ?: throw ApiError(ErrorCode.APP_CONFIG_MISSING)
        val verifier = verifiers[provider] ?: throw ApiError(ErrorCode.AUTH_PROVIDER_FAILED, "unknown provider")
        val v = verifier.verify(cfg, ctx.clientPlatform, req.idToken!!)

        data class Issued(val appUserId: String, val deviceSecret: String, val refresh: RefreshIssued, val access: String)
        val issued = txRunner.withTx(ctx) {
            val identityId = providerIdentityRepo.upsert(
                tid,
                UpsertInput(provider, v.accountId, v.email, v.emailVerified, v.phone, v.userMetadata,
                    loginIp = null, loginInstallId = ctx.installId, loginAppId = ctx.appId),
            )
            val appUserId = appUserRepo.ensure(ctx.appId, identityId)
            val existing = req.deviceSecret?.let { deviceSecretRepo.findValid(tid, it) }
            val (dsId, dsPlain) = if (existing != null && existing.authIdentityId == identityId) {
                deviceSecretRepo.touch(existing.id!!); existing.id!! to req.deviceSecret!!
            } else {
                deviceSecretRepo.issue(tid, identityId, ctx.installId)
            }
            val refresh = refreshRepo.issue(ctx.appId, appUserId, dsId, ctx.installId)
            Issued(appUserId, dsPlain, refresh, jwt.signAccess(appUserId, ctx.appId))
        }
        events.publishEvent(AuthLoggedInEvent(ctx.appId, issued.appUserId, ctx.installId))
        return LoginRes(issued.access, issued.refresh.token, issued.refresh.expiresAt, issued.deviceSecret,
            accessTtlSec, UserDto(issued.appUserId, v.email))
    }

    fun exchange(ctx: RequestContext, req: ExchangeReq): ExchangeRes {
        val tid = tenantId(ctx)
        val ds = deviceSecretRepo.findValid(tid, req.deviceSecret!!) ?: throw ApiError(ErrorCode.UNAUTHORIZED)
        if (ds.authTenantId != tid) throw ApiError(ErrorCode.UNAUTHORIZED)
        return txRunner.withTx(ctx) {
            val appUserId = appUserRepo.ensure(ctx.appId, ds.authIdentityId!!)
            deviceSecretRepo.touch(ds.id!!)
            val refresh = refreshRepo.issue(ctx.appId, appUserId, ds.id!!, null)
            ExchangeRes(jwt.signAccess(appUserId, ctx.appId), refresh.token, refresh.expiresAt,
                accessTtlSec, UserDto(appUserId, null))
        }
    }

    fun refresh(ctx: RequestContext, req: RefreshReq): RefreshRes {
        val row = refreshRepo.findByHash(ctx.appId, req.refreshToken!!) ?: throw ApiError(ErrorCode.UNAUTHORIZED)
        if (row.revokedAt != null) {                       // 重放：撤销该用户全部 refresh
            refreshRepo.revokeByAppUser(ctx.appId, row.appUserId!!)
            throw ApiError(ErrorCode.UNAUTHORIZED)
        }
        if (row.expiresAt?.isBefore(Instant.now()) != false) throw ApiError(ErrorCode.UNAUTHORIZED)
        val newId = ObjectId().toHexString()
        val won = refreshRepo.tryRotate(ctx.appId, row.tokenHash!!, newId)
        if (!won) throw ApiError(ErrorCode.UNAUTHORIZED)    // 并发失败方
        val refresh = refreshRepo.issue(ctx.appId, row.appUserId!!, row.deviceSecretId!!, row.loginInstallId, id = newId)
        return RefreshRes(jwt.signAccess(row.appUserId!!, ctx.appId), refresh.token, refresh.expiresAt, accessTtlSec)
    }

    fun logout(ctx: RequestContext, req: LogoutReq): LogoutRes {
        val row = refreshRepo.findByHash(ctx.appId, req.refreshToken!!)
        if (row?.deviceSecretId != null) {
            deviceSecretRepo.revoke(row.deviceSecretId!!)
            refreshRepo.revokeByDeviceSecret(row.deviceSecretId!!)   // 跨该设备所有 app
        }
        return LogoutRes(true)
    }

    fun me(ctx: RequestContext): MeRes {
        val userId = ctx.userId ?: throw ApiError(ErrorCode.UNAUTHORIZED)
        return MeRes(userId, null)   // v1 只回 id；email 需要时经 app_user→auth_identity 反查
    }
}
```

运行：`./gradlew :core-api:test --tests "*AuthServiceTest"`。预期 PASS。

- [ ] **步骤 3：Commit**：`git commit -am "feat(auth): AuthService flows"`

---

## 任务组 5：中间件 + RequestContext 集成 + 控制器 + JWKS 端点

### 任务 5.1：AuthInterceptor（populateAuth，非阻塞）

**文件：** 创建 `common/auth/AuthInterceptor.kt`；测试 `common/auth/AuthInterceptorTest.kt`

- [ ] **步骤 1：写失败测试 `AuthInterceptorTest`（MockHttpServletRequest，本地可跑）**

```kotlin
package com.ifmix.api.core.common.auth

import com.ifmix.api.core.common.http.RequestHeaders
import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test
import org.mockito.kotlin.mock
import org.mockito.kotlin.whenever
import org.springframework.mock.web.MockHttpServletRequest
import org.springframework.mock.web.MockHttpServletResponse

class AuthInterceptorTest {
    private val jwt = mock<AuthJwtService>()
    private val interceptor = AuthInterceptor(jwt)

    @Test fun `sets userId attribute when token valid and aid matches`() {
        whenever(jwt.verifyAccess("tok", "app1")).thenReturn("user1")
        val req = MockHttpServletRequest().apply {
            addHeader(RequestHeaders.APP_ID, "app1"); addHeader("Authorization", "Bearer tok")
        }
        assertThat(interceptor.preHandle(req, MockHttpServletResponse(), Any())).isTrue()
        assertThat(req.getAttribute(AuthInterceptor.ATTR_USER_ID)).isEqualTo("user1")
    }

    @Test fun `anonymous when no header - no throw, no attribute`() {
        val req = MockHttpServletRequest().apply { addHeader(RequestHeaders.APP_ID, "app1") }
        assertThat(interceptor.preHandle(req, MockHttpServletResponse(), Any())).isTrue()
        assertThat(req.getAttribute(AuthInterceptor.ATTR_USER_ID)).isNull()
    }

    @Test fun `anonymous when token invalid`() {
        whenever(jwt.verifyAccess("bad", "app1")).thenReturn(null)
        val req = MockHttpServletRequest().apply {
            addHeader(RequestHeaders.APP_ID, "app1"); addHeader("Authorization", "Bearer bad")
        }
        interceptor.preHandle(req, MockHttpServletResponse(), Any())
        assertThat(req.getAttribute(AuthInterceptor.ATTR_USER_ID)).isNull()
    }
}
```

运行：`./gradlew :core-api:test --tests "*AuthInterceptorTest"`。预期 FAIL。

- [ ] **步骤 2：实现 `AuthInterceptor`**

```kotlin
package com.ifmix.api.core.common.auth

import com.ifmix.api.core.common.http.RequestHeaders
import jakarta.servlet.http.HttpServletRequest
import jakarta.servlet.http.HttpServletResponse
import org.springframework.stereotype.Component
import org.springframework.web.servlet.HandlerInterceptor

/** populateAuth：非阻塞验签。有效 access + aid 匹配 → 请求属性存 appUserId；否则匿名（不报错）。 */
@Component
class AuthInterceptor(private val jwt: AuthJwtService) : HandlerInterceptor {

    override fun preHandle(request: HttpServletRequest, response: HttpServletResponse, handler: Any): Boolean {
        val appId = request.getHeader(RequestHeaders.APP_ID) ?: return true
        val auth = request.getHeader("Authorization") ?: return true
        if (!auth.startsWith("Bearer ")) return true
        val token = auth.removePrefix("Bearer ").trim()
        jwt.verifyAccess(token, appId)?.let { request.setAttribute(ATTR_USER_ID, it) }
        return true
    }

    companion object {
        const val ATTR_USER_ID = "com.ifmix.auth.userId"
    }
}
```

运行：`./gradlew :core-api:test --tests "*AuthInterceptorTest"`。预期 PASS。

### 任务 5.2：RequestContext 集成 + WebConfig 注册

**文件：** 修改 `common/http/RequestContextArgumentResolver.kt`、`common/config/WebConfig.kt`

- [ ] **步骤 1：`RequestContextArgumentResolver` 读 userId 属性**

在 `resolveArgument` 里把 `AuthInterceptor.ATTR_USER_ID` 请求属性读入 `userId`：

```kotlin
// import com.ifmix.api.core.common.auth.AuthInterceptor
// import org.springframework.web.context.request.RequestAttributes
override fun resolveArgument(...): Any = RequestContext(
    appId = header(webRequest, RequestHeaders.APP_ID) ?: "",
    installId = header(webRequest, RequestHeaders.INSTALL_ID),
    lang = header(webRequest, RequestHeaders.LANG),
    currency = header(webRequest, RequestHeaders.CURRENCY),
    country = header(webRequest, RequestHeaders.COUNTRY),
    clientPlatform = ClientPlatform.fromHeader(header(webRequest, RequestHeaders.CLIENT_PLATFORM)),
    userId = webRequest.getAttribute(AuthInterceptor.ATTR_USER_ID, RequestAttributes.SCOPE_REQUEST) as? String,
)
```

- [ ] **步骤 2：`WebConfig` 注册 AuthInterceptor（在 header 校验之后，仅 /customer）+ 排除 /.well-known**

```kotlin
@Configuration
class WebConfig(
    private val headerValidationInterceptor: HeaderValidationInterceptor,
    private val authInterceptor: com.ifmix.api.core.common.auth.AuthInterceptor,
) : WebMvcConfigurer {

    override fun addInterceptors(registry: InterceptorRegistry) {
        registry.addInterceptor(headerValidationInterceptor)
            .addPathPatterns("/customer/**", "/app/**")
            .excludePathPatterns("/v3/api-docs/**", "/swagger-ui/**", "/swagger-ui.html", "/.well-known/**")
        registry.addInterceptor(authInterceptor)
            .addPathPatterns("/customer/**")   // 认证装配仅消费端（admin 授权另议）
    }
    // addArgumentResolvers 不变
}
```

- [ ] **步骤 3：编译 + 跑 resolver/interceptor 相关测试**：`./gradlew :core-api:compileKotlin :core-api:compileTestKotlin`。

- [ ] **步骤 4：Commit**：`git commit -am "feat(auth): populateAuth interceptor + ctx.userId integration"`

### 任务 5.3：控制器 + JWKS 端点

**文件：** 创建 `bff/customer/CustomerAuthController.kt`、`bff/wellknown/JwksController.kt`

- [ ] **步骤 1：`CustomerAuthController`**

```kotlin
package com.ifmix.api.core.bff.customer

import com.ifmix.api.core.common.http.RequestContext
import com.ifmix.api.core.modules.auth.*
import jakarta.validation.Valid
import org.springframework.web.bind.annotation.*

@RestController
@RequestMapping("/customer/core")
class CustomerAuthController(private val authService: AuthService) {

    @PostMapping("/mutation/auth/google")
    fun google(ctx: RequestContext, @Valid @RequestBody req: LoginReq): LoginRes =
        authService.loginWithProvider(ctx, "google", req)

    @PostMapping("/mutation/auth/apple")
    fun apple(ctx: RequestContext, @Valid @RequestBody req: LoginReq): LoginRes =
        authService.loginWithProvider(ctx, "apple", req)

    @PostMapping("/mutation/auth/exchange")
    fun exchange(ctx: RequestContext, @Valid @RequestBody req: ExchangeReq): ExchangeRes =
        authService.exchange(ctx, req)

    @PostMapping("/mutation/auth/refresh")
    fun refresh(ctx: RequestContext, @Valid @RequestBody req: RefreshReq): RefreshRes =
        authService.refresh(ctx, req)

    @PostMapping("/mutation/auth/logout")
    fun logout(ctx: RequestContext, @Valid @RequestBody req: LogoutReq): LogoutRes =
        authService.logout(ctx, req)

    @PutMapping("/query/auth/me")
    fun me(ctx: RequestContext): MeRes = authService.me(ctx)   // ctx.userId 为空时 service 抛 UNAUTHORIZED
}
```

- [ ] **步骤 2：`JwksController`（公开，返回裸 JSON）**

```kotlin
package com.ifmix.api.core.bff.wellknown

import com.ifmix.api.core.common.auth.AuthJwtService
import org.springframework.http.MediaType
import org.springframework.web.bind.annotation.GetMapping
import org.springframework.web.bind.annotation.RestController

/** 公开 JWKS：供其它服务验签。无 header 校验（WebConfig 已 exclude /.well-known）。 */
@RestController
class JwksController(private val jwt: AuthJwtService) {

    @GetMapping("/.well-known/jwks.json", produces = [MediaType.APPLICATION_JSON_VALUE])
    fun jwks(): String = jwt.jwkSetJson()   // 返回 String，EnvelopeResponseAdvice 跳过包装
}
```

- [ ] **步骤 3：编译**：`./gradlew :core-api:compileKotlin`。预期 PASS。

- [ ] **步骤 4：Commit**：`git commit -am "feat(auth): controllers + jwks endpoint"`

---

## 任务组 6：mergeOnLogin + 集成点 + 装配

### 任务 6.1：MergeOnLoginListener（Spring @Async 事件）

**文件：** 创建 `modules/auth/MergeOnLoginListener.kt`；测试 `modules/auth/MergeOnLoginListenerTest.kt`（mock MongoTemplate + TxRunner）

- [ ] **步骤 1：写失败测试**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.http.RequestContext
import com.ifmix.api.core.common.tx.TxRunner
import com.ifmix.api.core.modules.antique.ScanRecordDocument
import com.ifmix.api.core.modules.iap.SubscriptionDocument
import com.mongodb.client.result.UpdateResult
import org.junit.jupiter.api.Test
import org.mockito.kotlin.*
import org.springframework.data.mongodb.core.MongoTemplate

class MergeOnLoginListenerTest {
    private val mongo = mock<MongoTemplate> {
        on { updateMulti(any(), any(), any<Class<*>>()) } doReturn UpdateResult.acknowledged(0, 0, null)
    }
    private val tx = mock<TxRunner> {
        on { withTx<Any>(any(), any()) } doAnswer { inv ->
            @Suppress("UNCHECKED_CAST") (inv.arguments[1] as (RequestContext) -> Any).invoke(inv.arguments[0] as RequestContext)
        }
    }
    private val listener = MergeOnLoginListener(mongo, tx)

    @Test fun `backfills scan and subscription when installId present`() {
        listener.onLogin(AuthLoggedInEvent("app1", "user1", "inst1"))
        verify(mongo).updateMulti(any(), any(), eq(ScanRecordDocument::class.java))
        verify(mongo).updateMulti(any(), any(), eq(SubscriptionDocument::class.java))
    }

    @Test fun `no-op when installId absent`() {
        listener.onLogin(AuthLoggedInEvent("app1", "user1", null))
        verifyNoInteractions(mongo)
    }
}
```

运行：`./gradlew :core-api:test --tests "*MergeOnLoginListenerTest"`。预期 FAIL。

- [ ] **步骤 2：实现**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.http.RequestContext
import com.ifmix.api.core.common.tx.TxRunner
import com.ifmix.api.core.modules.antique.ScanRecordDocument
import com.ifmix.api.core.modules.iap.SubscriptionDocument
import org.slf4j.LoggerFactory
import org.springframework.context.event.EventListener
import org.springframework.data.mongodb.core.MongoTemplate
import org.springframework.data.mongodb.core.query.Criteria
import org.springframework.data.mongodb.core.query.Query
import org.springframework.data.mongodb.core.query.Update
import org.springframework.data.mongodb.core.query.isEqualTo
import org.springframework.scheduling.annotation.Async
import org.springframework.stereotype.Component
import java.time.Instant

/**
 * 登录后归并匿名数据：把 (appId, installId, userId=null) 的 scan_record/subscription 回填 userId。
 * best-effort（错误隔离，不拖垮登录）；失败结构化 error 日志（便于告警/对账）。
 */
@Component
class MergeOnLoginListener(private val mongo: MongoTemplate, private val txRunner: TxRunner) {
    private val log = LoggerFactory.getLogger(javaClass)

    @Async
    @EventListener
    fun onLogin(e: AuthLoggedInEvent) {
        val installId = e.installId ?: return
        try {
            txRunner.withTx(RequestContext(appId = e.appId, installId = installId)) {
                val q = Query(
                    Criteria.where("appId").isEqualTo(e.appId)
                        .and("installId").isEqualTo(installId).and("userId").isEqualTo(null),
                )
                val u = Update().set("userId", e.appUserId).set("updatedAt", Instant.now())
                mongo.updateMulti(q, u, ScanRecordDocument::class.java)
                mongo.updateMulti(q, u, SubscriptionDocument::class.java)
            }
        } catch (ex: Exception) {
            log.error("mergeOnLogin failed appId={} appUserId={} installId={}", e.appId, e.appUserId, installId, ex)
        }
    }
}
```

运行：`./gradlew :core-api:test --tests "*MergeOnLoginListenerTest"`。预期 PASS。

- [ ] **步骤 3：Commit**：`git commit -am "feat(auth): mergeOnLogin listener"`

### 任务 6.2：限流主体收紧 D5（`userId ?? ip`）

**文件：** 修改 `common/ratelimit/TierResolver.kt`（`resolveSubject` 所在）；改对应单测。

- [ ] **步骤 1：改 `resolveSubject`**

```kotlin
/** subject 阶梯（D5）：登录按人(userId)、匿名按 ip；去掉 installId（客户端可控、易绕过限流）。 */
fun resolveSubject(ctx: RequestContext, ip: String?): String =
    ctx.userId ?: ip ?: "unknown"
```

- [ ] **步骤 2：更新 antique 相关单测**（若 `AntiqueServiceTest`/限流测试断言了 installId 作为 subject，改为 ip）。

运行：`./gradlew :core-api:test --tests "*Antique*" --tests "*RateLimiter*"`。预期 PASS。

- [ ] **步骤 3：Commit**：`git commit -am "refactor(ratelimit): subject userId ?? ip (D5)"`

### 任务 6.3：bean 装配 + env + @EnableAsync

**文件：** 创建 `modules/auth/AuthConfig.kt`；修改 `application.yml`、`CoreApplication.kt`（或 AuthConfig 加 @EnableAsync）

- [ ] **步骤 1：`AuthConfig`**

```kotlin
package com.ifmix.api.core.modules.auth

import com.ifmix.api.core.common.auth.AuthJwtKeys
import com.ifmix.api.core.common.auth.AuthJwtService
import com.ifmix.api.core.common.tx.TxRunner
import com.ifmix.api.core.modules.app.AppConfigRepo
import org.springframework.beans.factory.annotation.Value
import org.springframework.context.ApplicationEventPublisher
import org.springframework.context.annotation.Bean
import org.springframework.context.annotation.Configuration
import org.springframework.scheduling.annotation.EnableAsync
import org.springframework.security.oauth2.jwt.JwtDecoder
import org.springframework.security.oauth2.jwt.JwtValidators
import org.springframework.security.oauth2.jwt.NimbusJwtDecoder

@Configuration
@EnableAsync
class AuthConfig {

    @Bean
    fun googleJwtDecoder(): JwtDecoder =
        NimbusJwtDecoder.withJwkSetUri("https://www.googleapis.com/oauth2/v3/certs").build().apply {
            setJwtValidator(JwtValidators.createDefaultWithIssuer("https://accounts.google.com"))
        }

    @Bean
    fun appleJwtDecoder(): JwtDecoder =
        NimbusJwtDecoder.withJwkSetUri("https://appleid.apple.com/auth/keys").build().apply {
            setJwtValidator(JwtValidators.createDefaultWithIssuer("https://appleid.apple.com"))
        }

    @Bean
    fun providerVerifiers(googleJwtDecoder: JwtDecoder, appleJwtDecoder: JwtDecoder): Map<String, ProviderVerifier> =
        mapOf("google" to GoogleVerifier(googleJwtDecoder), "apple" to AppleVerifier(appleJwtDecoder))

    @Bean
    fun authJwtService(@Value("\${app.auth.jwt-private-key:}") privateJwk: String): AuthJwtService =
        AuthJwtService(AuthJwtKeys(privateJwk.ifBlank { null }), accessTtlSec = 900)

    @Bean
    fun authService(
        appConfigRepo: AppConfigRepo,
        providerVerifiers: Map<String, ProviderVerifier>,
        authJwtService: AuthJwtService,
        providerIdentityRepo: AuthProviderIdentityRepo,
        appUserRepo: AppUserRepo,
        deviceSecretRepo: AuthDeviceSecretRepo,
        refreshRepo: AppRefreshTokenRepo,
        txRunner: TxRunner,
        events: ApplicationEventPublisher,
    ): AuthService = AuthService(
        appConfigRepo, providerVerifiers, authJwtService, providerIdentityRepo, appUserRepo,
        deviceSecretRepo, refreshRepo, txRunner, events, accessTtlSec = 900,
    )
}
```
> `AuthJwtService` 与 `AuthConfig` 都写死 `accessTtlSec = 900`——抽成 `@Value("\${app.auth.access-ttl-sec:900}")`
> 传入两处以免漂移（DRY）：`authJwtService(privateJwk, ttl)` + `authService(..., ttl)` 共用同一注入值。

- [ ] **步骤 2：`application.yml` 加 env**

```yaml
app:
  auth:
    jwt-private-key: ${AUTH_JWT_PRIVATE_KEY:}   # Ed25519 OKP 的 JWK JSON；空则本地生成临时密钥（仅开发）
    access-ttl-sec: ${AUTH_ACCESS_TTL_SEC:900}
```

- [ ] **步骤 3：编译 + 全量非 Mongo 单测**

`./gradlew :core-api:compileKotlin :core-api:compileTestKotlin`
`./gradlew :core-api:test --tests "*Auth*" --tests "*EmailNormalize*" --tests "*Hashing*" --tests "*ProviderVerifier*" --tests "*MergeOnLogin*"`
预期 PASS。

- [ ] **步骤 4：Commit**：`git commit -am "feat(auth): bean wiring + env + @EnableAsync"`

### 任务 6.4：集成测试（Testcontainers Mongo 副本集，CI 跑）

**文件：** 创建 `modules/auth/AuthIntegrationTest.kt`（照写，本地仅 `compileTestKotlin`）

覆盖（用 stub `ProviderVerifier` 注入固定 `VerifiedProvider`，避免真实 Google/Apple）：
- login → 建 auth_identity + provider_identity + app_user + refresh + device_secret;同 provider 再登录复用 identity。
- exchange → 用 device_secret 为新 app 建 app_user + 令牌（不跑 provider）;跨租户 device_secret 拒。
- refresh 轮换：旧失效、新可用;并发双提交只一方成功（另一方 UNAUTHORIZED）;重放已撤销 → 撤销该用户全部 refresh。
- logout → 撤该 device_secret + 其关联全部 app 的 refresh。
- mergeOnLogin：预置匿名 scan_record/subscription（installId 命中）→ 登录后 userId 回填。
- 跨租户隔离：tenant2 持 tenant1 的 access（`aid` 不匹配）→ populateAuth 判匿名。

运行（有 Docker 时）：`./gradlew :core-api:test --tests "*AuthIntegrationTest"`。

- [ ] **步骤 5：Commit**：`git commit -am "test(auth): integration (testcontainers)"`

---

## 自检

**1. 规格覆盖度（对照 spec 章节）：**
- §1 身份分层/§2 数据模型 → 任务 1.2（6 文档）+ 1.4（repos）。
- §3 Token/device_secret → 任务组 2（JWT）+ 1.4（refresh/device_secret repo）。
- §4 流程（login/exchange/refresh/logout/me）→ 任务 4.2。
- §5 Provider 验证 → 任务组 3。
- §6 中间件 + JWKS 端点 → 任务 5.1/5.2/5.3。
- §7 mergeOnLogin（Spring 事件）→ 任务 6.1。
- §8 集成点（resolveSubject D5、appconfig 已就位、iap D1 已在 iap 计划）→ 任务 6.2（D5）；D1 无需改（iap 已实现双查）；fail-safe 日志属 antique/iap 既有 resolveTier，无新增。
- §9 错误处理 → `AUTH_PROVIDER_FAILED`（任务 3.1 步骤3）；`UNAUTHORIZED`/`APP_CONFIG_MISSING` 已存在。
- §10 测试 → 各任务单测 + 6.4 集成。
- §11 适配 / §12 拆分 → 本计划任务组结构即 §12 六段。

**2. 占位符扫描：** 无 TODO/待定；集成测试因无 Docker 标注"照写、CI 跑"（非占位，是环境约束）。

**3. 类型一致性核对：**
- `RefreshIssued(id, token, expiresAt)`、`AppRefreshTokenRepo.issue(...,id=null)`/`tryRotate`/`findByHash`/
  `revokeByAppUser`/`revokeByDeviceSecret`、`AuthDeviceSecretRepo.issue`(返回 `Pair<id,plain>`)/`findValid`/
  `touch`/`revoke`、`AppUserRepo.ensure`、`AuthProviderIdentityRepo.upsert(tid, UpsertInput)`、
  `AuthJwtService.signAccess/verifyAccess/jwkSetJson`、`ProviderVerifier.verify(config, platform, idToken)`→
  `VerifiedProvider`、`AuthService` 各方法签名 — 跨任务一致。
- `AuthInterceptor.ATTR_USER_ID` 常量在 interceptor 与 RequestContextArgumentResolver 一致。
- `AuthLoggedInEvent(appId, appUserId, installId?)` 在 service 发布、listener 消费一致。
- 依赖方向：auth → appconfig/antique/iap（编译期用其 repo/文档），单向无环。

## 执行交接

计划已完成并保存到 `docs/superpowers/plans/2026-07-27-07-auth.md`。两种执行方式：

1. **子代理驱动（推荐）** — 每个任务调度一个新子代理，任务间两阶段审查，快速迭代。
2. **内联执行** — 当前会话用 executing-plans 批量执行并设检查点。

选哪种方式？
